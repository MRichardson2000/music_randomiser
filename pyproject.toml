[project]
name = "music-randomiser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.14.3",
    "ty>=0.0.65",
]

[tool.hatch.build.targets.wheel]
packages = ["src/music_randomiser"]
